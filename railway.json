{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "start": "npm start",
  "env": {
    "NODE_ENV": "production"
  }
}
